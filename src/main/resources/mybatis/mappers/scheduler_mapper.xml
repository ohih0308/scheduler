<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohih.scheduler.scheduler.SchedulerMapper">
    <!--  int creatEvent(Event event);  -->
    <insert id="createEvent" parameterType="Event">
        INSERT INTO events
        (title, author_id, location, description, participants, start_date, end_date, start_time, end_time,
         all_day)
        VALUES (#{title}, #{authorId}, #{location}, #{description}, #{participants, typeHandler=com.ohih.scheduler.config.typehandler.ListTypeHandler},
                #{startDate}, #{endDate}, #{startTime}, #{endTime}, #{allDay})
    </insert>

</mapper>
